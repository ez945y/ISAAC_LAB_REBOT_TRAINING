# Shared infrastructure for the thesis benchmark experiments (RQ1-RQ4).
# Pure-stdlib where possible so the pipeline runs on any machine; the DAM
# filter itself needs torch/osqp/dam (Isaac machine).
